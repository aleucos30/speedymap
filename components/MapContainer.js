"use client";
import React from 'react';

export default function MapContainer({ viewport }) {
  return (
    <div className="w-full h-full bg-gray-900 flex items-center justify-center">
      <p className="text-gray-500">Mappa caricata (Placeholder)</p>
    </div>
  );
}
